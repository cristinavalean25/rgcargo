interface TransportProps {
  changeLanguage: (newLocale: string) => void;
  locale: string;
}

const TransportMasini: React.FC<TransportProps> = ({}) => {
  return <div>TransportMasini</div>;
};

export default TransportMasini;
